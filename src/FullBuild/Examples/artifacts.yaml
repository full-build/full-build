minversion: 2.5.0
binaries: C:\Binaries
nugets:
    - nuget: this should be a uri
test: nunit
vcs: git
mainrepository:
    uri: this should be a uri
repositories:
    - repo: id1
      branch: fullbuild
      build: msbuild
      uri: this should be a uri
    - repo: id1
      build: msbuild
      uri: this should be a uri
apps:
    - name: FullBuild
      type: copy
      projects:
        - project: fullbuild1
        - project: fullbuild2
