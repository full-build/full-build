minversion: 2.5.0
binaries: C:\Binaries
sxs: false
mainrepository:
    uri: this should be a uri
    vcs: git
repositories:
    - repo: id1
      branch: fullbuild
      build: msbuild
      uri: this should be a uri
      vcs: git
      test: nunit
    - repo: id1
      build: msbuild
      uri: this should be a uri
      vcs: git
      test: nunit