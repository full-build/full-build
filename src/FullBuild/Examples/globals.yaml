minversion: 2.5.0
binaries: C:\Binaries
nugets:
    - nuget: this should be a uri
vcs: git
test: nunit
mainrepository:
    uri: this should be a uri
repositories:
    - repo: id1
      branch: fullbuild
      build: msbuild
      uri: this should be a uri
    - repo: id1
      build: msbuild
      uri: this should be a uri
