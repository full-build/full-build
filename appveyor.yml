init:
  - git config --global core.autocrlf input
build_script:
  - msbuild: src/full-build.sln /p:Configuration=Release
test: off
version: 0.1.{build}
artifacts:
  - path: src/bin/Release
    name: bin